#!/bin/sh

# Évite un rendu logiciel Mesa accidentel.
unset LIBGL_ALWAYS_SOFTWARE
unset MESA_LOADER_DRIVER_OVERRIDE

# Utilise Xwayland/GLX pour le contexte OpenGL de SIFTGPU.
export QT_QPA_PLATFORM=xcb

exec colmap "$@"
