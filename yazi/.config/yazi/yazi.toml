[manager]
ratio           = [ 1, 4, 3 ]
show_hidden     = true        # Voir les fichiers .cachés
linemode        = "size"      # Nom + taille
sort_by         = "modified"  # Tri par date
sort_reverse    = true       # Plus récents en haut
sort_dir_first  = true     # Dossiers avant les fichiers
show_symlink    = true

[log]
enabled         = false           # Active à true en cas de debug

[[opener.pdf]]
run = 'evince "$@"'
block = false

[opener]
edit = [
	{ run = "${EDITOR:-vi} %s", desc = "$EDITOR",      for = "unix", block = true },
]
play = [
	{ run = "xdg-open %s1",    desc = "Play", for = "linux", orphan = true },
]
open = [
	{ run = "xdg-open %s1",    desc = "Open", for = "linux" },
]
reveal = [
	{ run = "xdg-open %d1",         desc = "Reveal", for = "linux" },
]
extract = [
	{ run = "ya pub extract --list %s", desc = "Extract here" },
]

[open]
rules = [
	# Folder
	{ url = "*/", use = [ "edit", "open", "reveal" ] },
	# Text
	{ mime = "text/*", use = [ "edit", "reveal" ] },
	# Image
	{ mime = "image/*", use = [ "open", "reveal" ] },
	# Media
	{ mime = "{audio,video}/*", use = [ "play", "reveal" ] },
	# Archive
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", use = [ "extract", "reveal" ] },
	# JSON
	{ mime = "application/{json,ndjson}", use = [ "edit", "reveal" ] },
	{ mime = "*/javascript", use = [ "edit", "reveal" ] },
	# Empty file
	{ mime = "inode/empty", use = [ "edit", "reveal" ] },
	# Virtual file system
	{ mime = "vfs/{absent,stale}", use = "download" },
	# Fallback
	{ url = "*", use = [ "open", "reveal" ] },
    # Pdf
    { mime = "application/pdf", use = "pdf" },
]

# A TOML linter such as https://taplo.tamasfe.dev/ can use this schema to validate your config.
# If you encounter any issues, please make an issue at https://github.com/yazi-rs/schemas.
"$schema" = "https://yazi-rs.github.io/schemas/yazi.json"

[mgr]
ratio          = [ 1, 4, 3 ]
sort_by        = "alphabetical"
sort_sensitive = false
sort_reverse 	 = false
sort_dir_first = true
sort_translit  = false
linemode       = "none"
show_hidden    = false
show_symlink   = true
scrolloff      = 5
mouse_events   = [ "click", "scroll" ]
title_format   = "Yazi: {cwd}"

[preview]
wrap            = "no"
tab_size        = 2
max_width       = 600
max_height      = 900
cache_dir       = ""
image_delay     = 30
image_filter    = "triangle"
image_quality   = 75
ueberzug_scale  = 1
ueberzug_offset = [ 0, 0, 0, 0 ]

[tasks]
micro_workers    = 10
macro_workers    = 10
bizarre_retry    = 3
image_alloc      = 536870912  # 512MB
image_bound      = [ 10000, 10000 ]
suppress_preload = false

[plugin]
prepend_fetchers = [
	{ id = "mime", url = "*/",         run = "mime.dir",    prio = "high", group = "mime" },
	{ id = "mime", url = "local://*",  run = "mime.local",  prio = "high", group = "mime" },
	{ id = "mime", url = "remote://*", run = "mime.remote", prio = "high", group = "mime" },
]

prepend_previewers = [
	{ url = "*/", run = "folder" },

	# Code / texte
	{ mime = "text/*", run = "code" },
	{ mime = "application/{mbox,javascript,wine-extension-ini}", run = "code" },

	# JSON
	{ mime = "application/{json,ndjson}", run = "json" },

	# Images
    { mime = "image/{avif,jxl}", run = "image" },
    { mime = "image/heic", run = "heic" },
    { mime = "image/heif", run = "heic" },  
	{ mime = "image/svg+xml", run = "image" },
	{ mime = "image/*", run = "image" },

	# Vidéo
	{ mime = "video/*", run = "video" },

	# PDF
	{ mime = "application/pdf", run = "pdf" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", run = "archive" },
	{ mime = "application/{debian*-package,redhat-package-manager,rpm,android.package-archive}", run = "archive" },
	{ url = "*.{AppImage,appimage}", run = "archive" },

	# Disques / images disque
	{ mime = "application/{iso9660-image,qemu-disk,ms-wim,apple-diskimage}", run = "archive" },
	{ mime = "application/virtualbox-{vhd,vhdx}", run = "archive" },
	{ url = "*.{img,fat,ext,ext2,ext3,ext4,squashfs,ntfs,hfs,hfsx}", run = "archive" },

	# Fallbacks spéciaux
	{ mime = "inode/empty", run = "noop" },
	{ mime = "vfs/*", run = "noop" },
	{ mime = "null/*", run = "noop" },
]

prepend_preloaders = [
	# Images
    { mime = "image/{avif,jxl}", run = "image", prio = "normal" },
    { mime = "image/heic", run = "heic", prio = "normal" },
    { mime = "image/heif", run = "heic", prio = "normal" },
	{ mime = "image/svg+xml", run = "image", prio = "normal" },
	{ mime = "image/*", run = "image", prio = "normal" },

	# Vidéos
	{ mime = "video/*", run = "video", prio = "normal" },

	# PDF
	{ mime = "application/pdf", run = "pdf", prio = "normal" },
]

[input]
cursor_blink    = false

# cd
cd_title        = "Change directory:"
cd_origin       = "top-center"
cd_offset       = [ 0, 2, 50, 3 ]

# create
create_title    = [ "Create:", "Create (dir):" ]
create_origin   = "top-center"
create_offset   = [ 0, 2, 50, 3 ]

# rename
rename_title    = "Rename:"
rename_origin   = "hovered"
rename_offset   = [ 0, 1, 50, 3 ]

# filter
filter_title    = "Filter:"
filter_origin   = "top-center"
filter_offset   = [ 0, 2, 50, 3 ]

# find
find_title      = [ "Find next:", "Find previous:" ]
find_origin     = "top-center"
find_offset     = [ 0, 2, 50, 3 ]

# search
search_title    = "Search via {n}:"
search_origin   = "top-center"
search_offset   = [ 0, 2, 50, 3 ]

# shell
shell_title     = [ "Shell:", "Shell (block):" ]
shell_origin    = "top-center"
shell_offset    = [ 0, 2, 50, 3 ]

[confirm]
# trash
trash_title 	    = "Trash {n} selected file{s}?"
trash_origin	    = "center"
trash_offset	    = [ 0, 0, 70, 20 ]

# delete
delete_title 	    = "Permanently delete {n} selected file{s}?"
delete_origin	    = "center"
delete_offset	    = [ 0, 0, 70, 20 ]

# overwrite
overwrite_title     = "Overwrite file?"
overwrite_body      = "Will overwrite the following file:"
overwrite_origin    = "center"
overwrite_offset    = [ 0, 0, 50, 15 ]

# quit
quit_title          = "Quit?"
quit_body           = "There are unfinished tasks, quit anyway?\n(Open task manager with default key 'w')"
quit_origin         = "center"
quit_offset         = [ 0, 0, 50, 15 ]

[pick]
open_title          = "Open with:"
open_origin         = "hovered"
open_offset         = [ 0, 1, 50, 7 ]

[which]
sort_by      	    = "none"
sort_sensitive      = false
sort_reverse 	    = false
sort_translit       = false


